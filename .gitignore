# === Terraform-specific ===
# Local .terraform directories
**/.terraform/*
# Terraform state files and backups
*.tfstate
*.tfstate.*
# Terraform lock files (dependency lock file)
**/.terraform.lock.hcl
# Lock info files generated by terraform apply
*.terraform.tfstate.lock.info
# Terraform variable files (often contain sensitive data like keys or passwords)

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version
# control as they are data points which are potentially sensitive and subject
# to change depending on the environment.
*.tfvars
*.tfvars.json
# Terraform override files (local customizations not meant for version control)
override.tf
override.tf.json
*_override.tf
*_override.tf.json
# Include override files explicitly if necessary for configurations or other case
# !example_override.tf
# Terraform plan output files
*.tfplan
# Terraform CLI configuration files
.terraformrc
terraform.rc

# === AWS-specific-important ===
# AWS credentials and configuration files (avoid exposing sensitive data)
**/.aws/credentials
**/.aws/config
*.env
*.env.example
*.envchainrc

# === macOS-specific ===
# macOS system files
**/.DS_Store
**/.AppleDouble
**/.LSOverride
**/.DocumentRevisions-V100
**/.fseventsd
**/.Spotlight-V100
**/.TemporaryItems
**/.Trashes
**/.VolumeIcon.icns
**/*dmg
**/*pkg

# Time Machine-related files
**/.com.apple.timemachine.donotpresent%
**/.com.apple.timemachine.*

# === Linux-specific ===
# Lost+found directory
**/lost+found/
*.tar.gz
*.tar.bz2
*.dev
*.rpm
*.gzip

# Temporary swap files
*.swp
*.swo

# === Common temporary or log files ===
# Logs and crash dumps
*.log
crash.*.log

# Temporary and backup files
*.tmp
*.bak
*.old
*.orig
*.~*

# Generated archives
*.zip
*.tar.gz
*.rar
*.7z

# === Node.js-specific ===

# Node modules folder
node_modules/

# === IDE/editor-specific ===
# VSCode
.vscode/
*.code-workspace
# JetBrains IDEs (IntelliJ, PyCharm, WebStorm, etc.)
.idea/
*.iml
# Sublime Text
*.sublime-*

# === Python-specific ===
# Python byte-compiled files
__pycache__/
*.py[cod]

# Virtual environments
.env/
.venv/

# === Git-specific ===
# Git conflict and merge files
*.orig
*.rej